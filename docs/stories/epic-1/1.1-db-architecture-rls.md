# Story 1.1: Database Architecture & RLS

## Status: ✅ Completed
## Epic: Epic 1 - Foundation & Governance

### User Role
As a **Database Architect**, I want to **implement the core schema and Row Level Security (RLS) policies** so that **the system has a robust foundation for multi-departmental travel management.**

### Description
Implement the base PostgreSQL schema in Supabase, ensuring that all tables (profiles, travel_requests, diem_rates, expenses) are created with proper constraints and that data access is strictly controlled by department and role via RLS.

### Acceptance Criteria
- [x] Create `profiles` table with automatic sync from Supabase Auth.
- [x] Create `travel_requests` table with fields for requester, destination, dates, and status.
- [x] Implement RLS policies:
    - [x] Users can only see their own requests.
    - [x] Managers (Chefia) can see requests from their department.
    - [x] DAD (Administrators) can see all requests.
- [x] Create `diem_rates` table for storing official rate values.

### Tasks
Detailed task breakdown: [1.1-tasks.md](file:///c:/Dev/Gestao-de-Viagens/docs/tasks/1.1-tasks.md)

### DoD (Definition of Done)
- [x] SQL Migrations applied successfully.
- [x] RLS policies verified by manual query tests.
- [x] No data leakage between departments.

### Dev Notes
- Migration: `20260222000000_sgpd_schema.sql`
- Uses Supabase `auth.uid()` and custom profile claims for RLS.

— *Orion, Master Orchestrator* 🎯
