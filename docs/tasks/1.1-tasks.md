# Task Breakdown: 1.1 Database Architecture & RLS

## Story Reference: [1.1-db-architecture-rls.md](file:///c:/Dev/Gestao-de-Viagens/docs/stories/epic-1/1.1-db-architecture-rls.md)
## Status: ✅ Completed

### T1.1.1: Core Schema Implementation
- **Description**: Define the essential tables for travel management.
- **Sub-tasks**:
    - [x] Create `profiles` table with `id`, `email`, `full_name`, `department`, and `role`.
    - [x] Create `travel_requests` table with status ENUM (`Draft`, `Pending`, `Approved`, `Rejected`).
    - [x] Create `diem_rates` table for rate lookups.
- **Input**: `schema.sql` design.
- **Output**: Migrated database schema in Supabase.

### T1.1.2: Row Level Security (RLS) Configuration
- **Description**: Secure data access based on user role and department.
- **Sub-tasks**:
    - [x] Enable RLS on all public tables.
    - [x] Implement policy: `profile_owner_access` (User can read/write own profile).
    - [x] Implement policy: `request_visibility` (User can see own requests; Managers see department requests).
    - [x] Implement policy: `admin_all_access` (DAD role bypasses filters).
- **Verification**: `SELECT` queries from different test users in the SQL Editor.

### T1.1.3: Database Functions & Triggers
- **Description**: Automate data consistency.
- **Sub-tasks**:
    - [x] Trigger for automatic profile creation on first Auth login.
    - [x] Function for calculating duration from request dates.
- **Output**: Stored procedures in `supabase/migrations`.

— *Orion, Master Orchestrator* 🎯
